{
  "name": "cinetex",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": ["core", "shared", "backend", "frontend"],
  "scripts": {
    "clean-all": "yarn workspaces run clean",
    "build-all": "yarn workspaces run build",
    "clean-build-all": "yarn workspaces run clean && yarn workspaces run build",
    "start-backend": "yarn workspace cinetex-backend start",
    "start-frontend": "yarn workspace cinetex-frontend start"
  }
}
